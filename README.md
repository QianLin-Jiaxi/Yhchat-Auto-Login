## 云湖账号自动登录📡
#### 此项目可以自动连接云湖wss，实现保持永久在线
#### 部署此项目后使用计划任务设置为一天触发一次即可
#### 切勿使用此功能进行非法活动！后果由使用此者承担
> **get参数key：若有内置访问密钥则请求时需附带此参数**

#### 返回数据：
```jsonn
{
  "success": true, // 是否成功
  "results": [
    {
      "userId": "123", // 用户ID
      "success": true, // 此账号是否成功
      "error": null // 错误信息
    }
    // 支持多个账号
  ]
}
```
